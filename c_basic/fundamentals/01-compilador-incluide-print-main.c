# include <stdio.h>
/*stdio.h:
standard = padrão
input = entrada
output = saída
.h (header)= diz que é um arquivo de cabeçalho
Obs: Sem ele o computador nem saberia sobre o printf
*/

int main() {
    printf("Minha primeira mensagem no terminal!\n");
    return 0;
}